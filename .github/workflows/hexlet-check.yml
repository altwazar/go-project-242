##########################################################################
#                    DO NOT DELETE OR EDIT THIS FILE                     #
#     This file is automatically generated and used to run tests         #
##########################################################################

name: hexlet-check

on:
  push:
    branches:
      - '**'
    tags:
      - '**'

jobs:
  # На гитхабе (нельзя использовать полный путь с https)
  build-github:
    if: ${{ github.server_url == 'https://github.com' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Hexlet project check
        uses: hexlet/project-action@release
        with:
          hexlet-id: ${{ secrets.HEXLET_ID }}


  # На forgejo runner (полный путь до экшена на гитхабе)
  # build-forgejo:
  #   if: ${{ github.server_url != 'https://github.com' }}
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Hexlet project check
  #       uses: https://github.com/hexlet/project-action@release
  #       with:
  #         hexlet-id: ${{ secrets.HEXLET_ID }}
